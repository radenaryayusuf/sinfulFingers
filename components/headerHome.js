/**
 * Button component
 * Renders a button and calls a function passed via onPress prop once tapped
 */

import React, { Component } from "react";
import { Container, Header,Button,Footer,FooterTab, Left, Body ,Thumbnail, Right, Title, StyleProvider } from 'native-base';
import {Image,Text,StatusBar,View,ImageBackground} from 'react-native'
import { Col, Row, Grid } from "react-native-easy-grid";
import Icon from 'react-native-vector-icons/FontAwesome';

export default class headerHome extends Component {
  render() {
    return (
     <Text>asdasd</Text>
     
    );
  }
}

